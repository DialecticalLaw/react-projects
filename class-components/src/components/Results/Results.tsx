import { Component, ReactNode } from 'react';
import styles from './Results.module.css';

export class Results extends Component {
  render(): ReactNode {
    return <section className={styles.results_section}>results</section>;
  }
}
